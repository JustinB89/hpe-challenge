/*
 * malloc-2.7.2.c -- Doug Lea's malloc, version 2.7.2
 *
 * IMPORTANT: This is a STUB placeholder.
 *
 * Replace this file with the real malloc-2.7.2.c from the contest starter
 * kit before pushing for grading.  The real file is shipped with the
 * Dockerfile starter kit and is required by contest rules.
 *
 * With this stub in place the build compiles correctly and the system
 * allocator (glibc malloc) is used instead.  That is fine for local
 * development but will NOT match the grading environment.
 *
 * How to get the real file:
 *   cp <starter-kit>/build/malloc-2.7.2.c  build/malloc-2.7.2.c
 *
 * The stub intentionally defines no symbols so there is no conflict with
 * the system allocator.
 */
